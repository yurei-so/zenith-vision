# UI layout baseline

This CPU-only experiment evaluates conservative crop proposals for a synthetic
default Guild Wars 2 HUD fixture. It establishes the metrics and result schema
that learned region detectors must beat. The hard-coded proposal confidence is
intentionally low: geometry is not semantic detection.
