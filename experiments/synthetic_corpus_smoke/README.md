# Synthetic corpus smoke

This CPU-only experiment generates a temporary text-free HUD corpus, validates
every content digest and privacy declaration, verifies the immutable split, and
deletes the corpus when the process exits. No operator pixels are used.
