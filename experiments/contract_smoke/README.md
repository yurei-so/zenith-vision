# Contract smoke

This CPU-only probe checks the fixed Agent Runtime entrypoint and the core
fusion invariant. It reads committed synthetic fixtures only. It does not
capture a screen, connect to Guild Wars 2, contact a model, or acquire a GPU.
